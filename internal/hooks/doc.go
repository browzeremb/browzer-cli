// Package hooks provides shared types and constants for the
// browzer hook subcommand group. Each guard is implemented in
// packages/cli/internal/commands/hooks/ and depends on this package
// for the exit-code protocol.
package hooks
