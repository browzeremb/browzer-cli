# Goreleaser output — built locally via `goreleaser release --snapshot`
# or in CI. Never committed; the published artifacts live as GitHub
# Releases in the public mirror repo.
dist/

# Mutation testing output — generated by `make mutate` (go-mutesting).
# Reports land here after nightly CI or manual runs.
mutate-out/
